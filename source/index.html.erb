---
title: "Ritchie Macapinlac - Personal Blog & Digital Journal"
description: "Thoughtful reflections on technology, parenting, productivity, and personal development. A digital journal by Ritchie Macapinlac exploring life, work, and everything in between."
---

<!-- Hero Section -->
<section class="hero">
  <div class="hero-content">
    <h1 class="hero-title"><%= data.homepage_hero.title %></h1>
    <p class="hero-tagline"><%= data.homepage_hero.tagline %></p>
    <p class="hero-description">
      <%= data.homepage_hero.description %>
    </p>
    <div class="hero-actions">
      <%= link_to data.homepage_hero.primary_button.text, data.homepage_hero.primary_button.url, class: 'button button--primary' %>
      <%= link_to data.homepage_hero.secondary_button.text, data.homepage_hero.secondary_button.url, class: 'button button--secondary' %>
    </div>
  </div>
</section>

<!-- Featured Content Section -->
<section class="featured-content">
  <div class="container">
    <h2 class="section-title">Featured Series</h2>
    <div class="featured-grid">
      <% if all_series_metadata.any? %>
        <% all_series_metadata.first(2).each do |series_metadata| %>
          <% articles = series_articles(series_metadata[:name]) %>
          <% if articles.any? %>
            <%= partial 'layouts/_series_card', locals: { 
              series_name: series_metadata[:name], 
              articles: articles,
              series_metadata: series_metadata 
            } %>
          <% end %>
        <% end %>
      <% else %>
        <article class="card">
          <div class="card-content">
            <h2 class="card-title">No Series Available</h2>
            <p class="card-description">
              Check back soon for multi-part content series and explorations.
            </p>
            <div class="card-meta">
              <span class="card-count">Coming soon</span>
            </div>
            <%= link_to "Browse All Posts", '/writing/', class: 'button button--primary' %>
          </div>
        </article>
      <% end %>
    </div>
  </div>
</section>

<!-- Site Introduction Section -->
<section class="site-intro">
  <div class="container">
    <div class="intro-content">
      <h2 class="section-title">About This Site</h2>
      <p class="intro-text">
        This is my digital journal—a place where I share thoughts on technology, parenting, productivity, and personal development. 
        I write about what I'm learning, experimenting with, and thinking about. From Docker guides to EV road trips, 
        from parenting insights to productivity experiments, this is where I document my journey.
      </p>
      <p class="intro-text">
        I believe in thoughtful, authentic writing that balances professional insights with personal experience. 
        Whether you're here for the tech content, parenting reflections, or productivity tips, I hope you find something valuable.
      </p>
      <div class="intro-actions">
        <%= link_to 'Read My Latest Posts', '/', class: 'button button--secondary' %>
        <%= link_to 'Browse All Series', '/series', class: 'button button--tertiary' %>
      </div>
    </div>
  </div>
</section>

<!-- Recent Posts Section -->
<section class="recent-posts">
  <div class="container">
    <h2 class="section-title">Recent Posts</h2>
    <div class="posts-grid">
      <% blog.articles.first(6).each_with_index do |article, i| %>
        <%= partial 'layouts/_blog_card_homepage', locals: {
          article: article,
          i: i
        } %>
      <% end %>
    </div>
    
    <% if blog.articles.length > 6 %>
      <div class="posts-actions">
        <%= link_to 'View All Posts', '/writing/', class: 'button button--primary' %>
      </div>
    <% end %>
  </div>
</section>
