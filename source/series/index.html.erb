---
layout: layout
title: "Series - macapinlac.com"
description: "Multi-part content series and collections from Ritchie Macapinlac"
---

<div class="page-container">
  <header class="page-header">
    <h1 class="page-title">Series</h1>
  </header>

  <div class="page-content">
    <% if all_series_metadata.empty? %>
      <div class="empty-state">
        <p>No series content available yet. Check back soon for multi-part explorations!</p>
      </div>
    <% else %>
      <div class="card-grid">
        <% all_series_metadata.each do |series_metadata| %>
          <% articles = series_articles(series_metadata[:name]) %>
          <% if articles.any? %>
            <%= partial 'layouts/_series_card', locals: { 
              series_name: series_metadata[:name], 
              articles: articles,
              series_metadata: series_metadata 
            } %>
          <% end %>
        <% end %>
      </div>
    <% end %>
  </div>
</div> 